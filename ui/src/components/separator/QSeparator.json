{
  "meta": {
    "docsUrl": "https://v2.quasar.dev/vue-components/separator"
  },

  "props": {
    "dark": {
      "extends": "dark"
    },

    "spaced": {
      "type": [ "Boolean", "String" ],
      "desc": "如果设置为 'true'，相应的方向边距将被设置为8px；它也可以设置为以CSS为单位的尺寸，包括单位名称，或者xs|sm|md|lg|xl预定义的尺寸之一",
      "default": "md",
      "examples": [
        "12px", "sm", "md"
      ],
      "category": "content"
    },

    "inset": {
      "type": [ "Boolean", "String" ],
      "desc": "如果设置为布尔值 'true'，左右边距将被设置为16px。如果设置为 'item'，那么它将匹配一个 QItem 的设计。如果设置为 'item-thumbnail'，那么它将与一个 QItem 的设计相匹配，在左边有一个缩略图",
      "examples": [ "item", "item-thumbnail" ],
      "category": "content"
    },

    "vertical": {
      "type": "Boolean",
      "desc": "如果设置为 'true'，分隔符将是垂直的。",
      "category": "content"
    },

    "size": {
      "extends": "size"
    },

    "color": {
      "extends": "color"
    }
  }
}
