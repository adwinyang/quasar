{
  "meta": {
    "docsUrl": "https://v2.quasar.dev/vue-components/no-ssr"
  },

  "props": {
    "tag": {
      "extends": "tag",
      "default": "div",
      "examples": [ "div", "span", "blockquote" ]
    },

    "placeholder": {
      "type": "String",
      "desc": "在服务器端渲染时显示的文本（除非使用 placeholder(占位符) 插槽）。",
      "examples": [ "This is server-side only" ],
      "category": "content"
    }
  },

  "slots": {
    "default": {
      "desc": "默认插槽用于在客户端渲染内容"
    },

    "placeholder": {
      "desc": "在服务器端渲染时用作占位符的插槽，在客户端被默认的插槽取代；覆盖 'placeholder'(占位符) 属性。"
    }
  }
}
