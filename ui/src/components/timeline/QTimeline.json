{
  "meta": {
    "docsUrl": "https://v2.quasar.dev/vue-components/timeline"
  },

  "props": {
    "color": {
      "extends": "color"
    },

    "side": {
      "type": "String",
      "desc": "在密集和舒适的布局中放置时间线条目；对于松散的布局，它被QTimelineEntry边上的属性所覆盖。",
      "default": "right",
      "values": [ "left", "right" ],
      "category": "behavior"
    },

    "layout": {
      "type": "String",
      "desc": "时间线的布局。密集型将内容和标签放在一边。舒适的将内容放在一侧，标签放在另一侧。松散的把内容放在两边。",
      "default": "dense",
      "values": [ "dense", "comfortable", "loose" ],
      "category": "behavior"
    },

    "dark": {
      "extends": "dark"
    }
  },

  "slots": {
    "default": {
      "desc": "用于组件的内容"
    }
  }
}
