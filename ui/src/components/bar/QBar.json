{
  "meta": {
    "docsUrl": "https://v2.quasar.dev/vue-components/bar"
  },

  "props": {
    "dense": {
      "extends": "dense"
    },

    "dark": {
      "extends": "dark",
      "desc": "组件的背景颜色会照亮父体的背景（与默认行为相反，默认行为是使其变暗）；除非你为其指定一个CSS背景颜色，否则会发挥作用。"
    }
  },

  "slots": {
    "default": {
      "extends": "default"
    }
  }
}
